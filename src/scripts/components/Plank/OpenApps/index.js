import React, {Component} from "react"
import {OpenContainer} from "./styled"

class OpenApps extends Component {
  render() {
    return (
      <OpenContainer>
      </OpenContainer>
    )
  }
}

export default OpenApps